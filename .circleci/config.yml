version: 2
jobs:
  build:
    docker:
      - image: circleci/android:api-27-alpha
    steps:
      - checkout
      - run:
        name: "Clean & Build Project"
        command: ./gradlew clean build