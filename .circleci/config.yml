version: 2
jobs:
  build:
    docker:
      - image: circleci/android:api-27-alpha
    steps:
      - checkout
      - run: gem install fastlane
      - run: fastlane test
  deploy:
     docker:
       - image: circleci/android:api-27-alpha
     steps:
       - run: echo "Deployed"
workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - hold:
          type: approval
          requires:
           - build
      - deploy:
          requires:
            - hold